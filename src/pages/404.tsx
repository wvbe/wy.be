import { FC } from 'react';
import './index/index.css';

import RootLayout from '@/app/layout';

const Page: FC = () => {
	return (
		<RootLayout>
			<h1>Page not found</h1>
		</RootLayout>
	);
};

export default Page;
